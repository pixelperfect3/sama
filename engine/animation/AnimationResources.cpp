#include "engine/animation/AnimationResources.h"

// All methods are currently inline in the header.
// This translation unit exists so the engine_animation static library
// has at least one .cpp file and links correctly on all platforms.

namespace engine::animation
{
}  // namespace engine::animation
