diff --git a/src/renderer_vk.cpp b/src/renderer_vk.cpp
index abcdef1..1234567 100644
--- a/src/renderer_vk.cpp
+++ b/src/renderer_vk.cpp
@@ -4235,7 +4235,12 @@
 		}
 	};

-#define NUM_SWAPCHAIN_IMAGE 4
+	// NUM_SWAPCHAIN_IMAGE: Maximum number of swapchain images bgfx can handle.
+	// Modern Android devices (e.g., Pixel 9 with Mali GPU, Vulkan 1.3) may
+	// request minImageCount >= 5 from vkGetPhysicalDeviceSurfaceCapabilitiesKHR.
+	// Default raised from 4 to 8 to accommodate these devices. Override at
+	// compile time with -DNUM_SWAPCHAIN_IMAGE=N if needed.
+#ifndef NUM_SWAPCHAIN_IMAGE
+#define NUM_SWAPCHAIN_IMAGE 8
+#endif

 	struct SwapChainVK
 	{
