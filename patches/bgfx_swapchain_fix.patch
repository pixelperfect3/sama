diff --git a/src/renderer_vk.cpp b/src/renderer_vk.cpp
index aff49c27f..faf79d7b8 100644
--- a/src/renderer_vk.cpp
+++ b/src/renderer_vk.cpp
@@ -15,6 +15,10 @@
 #	import <Metal/Metal.h>
 #endif // BX_PLATFORM_OSX
 
+#ifndef NUM_SWAPCHAIN_IMAGE
+#define NUM_SWAPCHAIN_IMAGE 8
+#endif
+
 namespace bgfx { namespace vk
 {
 	static char s_viewName[BGFX_CONFIG_MAX_VIEWS][BGFX_CONFIG_MAX_VIEW_NAME];
@@ -2028,7 +2032,16 @@ VK_IMPORT_DEVICE
 				m_sci.pNext = NULL;
 				m_sci.flags = 0;
 				m_sci.surface = m_surface;
-				m_sci.minImageCount   = surfaceCapabilities.minImageCount;
+				// Clamp to array capacity — see patches/bgfx_swapchain_fix.patch
+				uint32_t _desiredImages = surfaceCapabilities.minImageCount;
+				if (surfaceCapabilities.maxImageCount > 0 && _desiredImages > surfaceCapabilities.maxImageCount)
+					_desiredImages = surfaceCapabilities.maxImageCount;
+				if (_desiredImages > NUM_SWAPCHAIN_IMAGE)
+				{
+					BX_TRACE("Warning: device minImageCount %d > NUM_SWAPCHAIN_IMAGE %d, clamping.", _desiredImages, (int)NUM_SWAPCHAIN_IMAGE);
+					_desiredImages = NUM_SWAPCHAIN_IMAGE;
+				}
+				m_sci.minImageCount   = _desiredImages;
 				m_sci.imageFormat     = m_backBufferColorFormat.format;
 				m_sci.imageColorSpace = m_backBufferColorFormat.colorSpace;
 				m_sci.imageExtent.width  = width;
@@ -4235,7 +4248,7 @@ VK_IMPORT_DEVICE
 			vkFreeCommandBuffers(m_device, m_commandPool, 1, &commandBuffer);
 		}
 
-#define NUM_SWAPCHAIN_IMAGE 4
+#define NUM_SWAPCHAIN_IMAGE 8
 		VkAllocationCallbacks*   m_allocatorCb;
 		VkDebugReportCallbackEXT m_debugReportCallback;
 		VkInstance       m_instance;
